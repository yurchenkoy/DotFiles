# Point everything at the Proton Pass desktop app's SSH agent socket.
# Proton Pass serves the git signing key from its vault while the vault is unlocked.
SSH_AUTH_SOCK=${HOME}/.ssh/proton-pass-ssh-agent.sock
